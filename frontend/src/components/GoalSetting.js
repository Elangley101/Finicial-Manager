import React from 'react';

const GoalSetting = () => {
    return (
        <div className="goal-setting">
            <h2>Financial Goals</h2>
            {/* Add UI for setting and tracking goals */}
        </div>
    );
};

export default GoalSetting;
