import React from 'react';

const Reports = () => {
    return (
        <div className="reports">
            <h2>Reports</h2>
            {/* Add UI for generating and viewing reports */}
        </div>
    );
};

export default Reports;
